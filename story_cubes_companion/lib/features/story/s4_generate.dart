import 'package:flutter/material.dart';

class S4Generate extends StatelessWidget {
  const S4Generate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Generate')),
      body: const Center(child: Text('Full Story or Prompt Pack output')),
    );
  }
}
